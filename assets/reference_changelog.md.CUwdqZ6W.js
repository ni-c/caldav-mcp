import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CegG7-6m.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1788608199000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="unreleased" tabindex="-1"><a href="https://github.com/ni-c/caldav-mcp/compare/v0.1.4...HEAD" target="_blank" rel="noreferrer">Unreleased</a> <a class="header-anchor" href="#unreleased" aria-label="Permalink to “Unreleased”">​</a></h2><h2 id="_0-1-4-2026-09-08" tabindex="-1"><a href="https://github.com/ni-c/caldav-mcp/releases/tag/v0.1.4" target="_blank" rel="noreferrer">0.1.4</a> - 2026-09-08 <a class="header-anchor" href="#_0-1-4-2026-09-08" aria-label="Permalink to “0.1.4 - 2026-09-08”">​</a></h2><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><strong>A document wrapped in <code>&lt;![CDATA[…]]&gt;</code> is read instead of counted as unreadable.</strong> <code>calendar-data</code> is a <code>stopNodes</code> entry, so the parser hands back its raw source rather than its text — and the XML packaging comes with it. Open-Xchange wraps the payload that way, which in the sibling server (carddav-mcp, against the same mailbox.org account) meant an address book of 79 cards listing as empty. CalDAV there happens to serve the payload unwrapped, so this is the dialect this server has not met yet rather than one it can rule out. The sections are now taken off and joined, including the split a server has to make around a document containing <code>]]&gt;</code>. Entity references <strong>inside</strong> a section are left alone, because that is what a CDATA section means.</li><li><strong>An indented response is read too.</strong> <code>trimValues</code> does not reach a stop node, so a server that pretty-prints its XML handed over <code>\\n BEGIN:VCALENDAR…</code>, and an iCalendar document has to start at <code>BEGIN:</code>.</li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><strong>A CR or LF reference is decoded where a real newline follows it.</strong> They used to be refused everywhere in <code>calendar-data</code>, to stop <code>SUMMARY:harmless&amp;#13;&amp;#10;ATTENDEE;PARTSTAT=ACCEPTED:mailto:x</code> becoming two properties. That still holds — a reference smuggled into a value has no real newline behind it and stays literal. What is new is the other shape: a server that encodes its line endings writes the reference immediately before the newline it stands for, because XML normalises a raw CR to LF on the way in and escaping it is the only way to keep it. sabre/dav does exactly that, and refusing it made every card unreadable in carddav-mcp until 0.1.0. The two servers disagreed about this node and only one of them could have been right; they now apply the same rule in the same words.</li><li><code>mcp-tool-allowlist</code> 0.2.2, <code>oxlint</code> 1.82.</li></ul><h2 id="_0-1-3-2026-09-07" tabindex="-1"><a href="https://github.com/ni-c/caldav-mcp/releases/tag/v0.1.3" target="_blank" rel="noreferrer">0.1.3</a> - 2026-09-07 <a class="header-anchor" href="#_0-1-3-2026-09-07" aria-label="Permalink to “0.1.3 - 2026-09-07”">​</a></h2><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><p>The second internal hardening pass, 2026-09-07. Every item below was a real defect in a tree that had passed <code>npm audit</code>, Dependabot, CodeQL, Scorecard and Trivy, and each ships with a test that asserts on the wire or on the result.</p><ul><li><strong><code>mcp-approval</code> 0.8.1: a sealed dialog answer is single-use.</strong> On protocol revision <code>2026-07-28</code> the dialog is a return value, and with 0.8.0 the same <code>requestState</code> and the same ticked box executed the edit again for as long as the state lived — fifteen minutes — and the key for changing a whole series is the same on every call, which is what made every replay land. A test drives the 2026 revision through the in-memory transport and pins that a replayed state asks again and writes nothing.</li><li><strong>A stored value that is not an integer no longer fails the whole answer.</strong> The output schema promises <code>int()</code> for <code>sequence</code>, <code>priority</code> and <code>percent_complete</code>, and the SDK fails the <em>whole call</em> when a result breaks that promise. ical.js hands <code>PERCENT-COMPLETE;VALUE=FLOAT:1.5</code> over as 1.5, a twenty-digit <code>SEQUENCE</code> as 1e20, and an <code>ATTACH;SIZE=</code> of three hundred digits reaches <code>Number()</code> as Infinity — so one such line, written by anybody with access to a shared calendar, took every listing it appeared in down. <code>sequence</code> sits in every kind, so <code>list_events</code>, <code>list_tasks</code> and <code>list_journals</code> alike. The field is left out now; a <code>SEQUENCE</code> this server cannot read restarts at 1 on the next write. A property test over arbitrary property values pins the whole class, and its first run found two more: an alarm <code>TRIGGER:0</code>, which ical.js throws on when first read — after the per-entry guard, so the listing died — and <code>SEQUENCE:-0</code>, on which the two halves of a result disagreed.</li><li><strong>An approval is keyed on a tuple, not a set.</strong> The library&#39;s resource key sorts its parts, which is right for a set and wrong for (from, to): the key for moving <code>e.ics</code> from Work to Private was the key for moving the <code>e.ics</code> in Private to Work, so a token issued for the one authorised the other. Every part now carries its position: the keys are built with <code>orderedResourceKey</code>, which this release takes from mcp-approval 0.8.2.</li><li><strong>The status is decided before the body is read, and a <code>401</code> is remembered.</strong> Every verb read the body under the success ceiling and only then looked at the status, so a reverse proxy answering <code>401</code> with a two-megabyte login page surfaced as &quot;larger than 1048576 bytes and was refused&quot;. An error body is now read up to 64 KiB and cut there. And because every request is a login and a hosted provider locks an account after a handful of failed ones, a <code>401</code> is repeated from memory for ten seconds — the repeated error says how old it is and what to check — so a model retrying a tool annotated cheap and read-only cannot lock the account.</li><li><strong>A tool call has a budget as a whole.</strong> One REPORT per calendar, thirty seconds allowed for each, over every calendar the credentials can see: a slow server with a hundred collections turned one call into an hour with no way to say so. A call now has a 30-second budget checked before each request, a ceiling of 5000 collected occurrences, and an answer that says after how many of how many calendars it stopped. Discovery has the same shape one level up — eight home sets, twenty addresses, 256 calendars, each overflow counted and reported — and a collection a home set lists is used only if it sits under that home set.</li><li><strong>Every string walk is linear at its ceiling.</strong> <code>/\\/+$/</code> and <code>/[^/]*$/</code> are tried from every position of a run and consume the run each time: eighty thousand slashes followed by one character cost two seconds, from any href the server chose, and a segment filling the 16 MiB multistatus ceiling would have cost hours. A counted walk and <code>lastIndexOf</code> replace them, an href past 8 KiB is refused before anything walks it, and <code>linear-time.test.ts</code> times every such function at its ceiling.</li><li><strong>An ETag is held to the RFC 9110 grammar before it becomes an <code>If-Match</code>.</strong> undici refuses a header value with a control character, which reached the model as <code>fetch failed</code> on every write to that resource for as long as the server kept sending it. An ETag that is not an entity-tag is treated like a weak one, which the write path already refuses with a sentence.</li><li><strong>A recurrence rule and an alarm trigger are checked as values, not only parsed.</strong> ical.js reads what it understands and drops the rest: <code>COUNT=1e9</code> became <code>COUNT=1</code>, <code>INTERVAL=0</code> and an unknown part vanished, the second of two <code>COUNT</code>s won, everything after a line break was gone; <code>PT-5M</code> was written verbatim, <code>P1W2D</code> as <code>P9D</code>, a hundred billion hours accepted. Each was then written into a shared calendar as if it were what the caller asked for. Both are held to their RFC 5545 grammar and the rule is compared part by part with what would be written. The test for the accepted triggers found that every absolute reminder went out with <code>VALUE=DATE-TIME</code> twice on one property, which is not iCalendar.</li><li><strong>An untyped error is quoted like every other value an error repeats.</strong> ical.js writes the offending value into its messages, and the last catch in <code>run()</code> repeated that text to the model whole, in this server&#39;s own voice, outside any fence.</li><li><strong>The fenced text block of a single entry is measured as emitted</strong>, with the datamark on every line, against the same ceiling as the JSON beside it; it used to go out unmeasured, so an entry just under the ceiling was answered three times over. The budget also descends one level, so an attendee list under <code>event</code> can be shortened instead of the whole answer refused.</li><li><strong>An allowlist entry that matched nothing is no longer printed in full.</strong><code>CALDAV_CALENDARS</code> sits one line below <code>CALDAV_PASSWORD</code> in every compose file, and an entry that matches nothing is what a secret pasted into the wrong line looks like. Only a value shaped like a calendar reference is quoted; the rest is described by its length, to stderr and to the model alike, and so are the other three &quot;got …&quot; diagnostics.</li><li><strong>The publish job installs with <code>--ignore-scripts</code>.</strong> It is the job with <code>id-token: write</code> for npm Trusted Publishing, and every dependency&#39;s install hook ran while that token was available. <code>gh release create</code> verifies the tag; a <code>dependency-review</code> job checks what a pull request changes.</li><li><strong>The runtime image no longer carries yarn, corepack or a lockfile.</strong> npm and npx had been removed; the other two package managers stayed behind.</li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>A <code>404</code> or a <code>405</code> on a discovery guess — the origin root behind a <code>/dav.php</code> prefix is usually a plain web server — ended discovery one step before the home-set fallback written for exactly that case. Those two statuses now degrade like an HTML page; a <code>401</code>, a <code>403</code> and a <code>5xx</code> are still the answer.</li><li>A calendar&#39;s id is printed byte for byte, and the same in every tool. <code>list_calendars</code> ran the path through the text cleaner, which rewrote <code>![a](b)</code>, normalised to NFKC and cut a long path — an id no tool could resolve — while <code>get_server_info</code> printed the raw path beside it. An identifier that has to round-trip is validated, not cleaned. The same sweep cleaned what <code>get_server_info</code> repeats from the server and the attendee <code>ROLE</code>, <code>PARTSTAT</code> and attachment <code>FMTTYPE</code> in a listing.</li><li><code>is_self</code> is decided the same way in every tool. <code>CALDAV_USER_EMAIL</code> was lower-cased for listings and not for <code>get_event</code> or <code>get_server_info</code>.</li><li>A URL-form <code>CALDAV_CALENDARS</code> entry matches only on the configured origin; an entry for another account no longer stands in for the same path on this one.</li><li>The id tag was looked up in an object literal, where <code>constructor</code> is a key too, and the sentence built from it said &quot;the id of a undefined&quot;.</li><li><code>CALDAV_URL</code> is kept as the parsed origin and path rather than as the environment string, so the whitespace a copied line carries is no longer glued in front of every path.</li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>The server introduces itself in full. <code>title</code>, <code>description</code>, <code>websiteUrl</code> and <code>icons</code> now travel with <code>name</code> and <code>version</code>, so a client that shows a server to a person has something to show. All four were already in <code>server.json</code> for the registry and reached no client at all; a test compares the two so they cannot drift.</li><li>An OpenSSF Scorecard run, weekly and on every push to <code>main</code>, reporting into the Security tab next to CodeQL and Trivy. The badge is the second in the row.</li><li>The demo recording is embedded in the README and on the docs home page. It had been rendered and shipped since 0.1.0 and was linked from neither.</li><li>Four test suites: the discovery walk shape by shape, every approval outcome and every write-path refusal, the insecure-TLS switch with undici mocked, and the property tests above. The coverage floor rises to 97 / 88 / 99 / 98.</li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li>oxlint&#39;s <code>suspicious</code> category is on. Thirty of its thirty-nine findings were <code>sort()</code> on a fresh array in a test, which <code>toSorted()</code> says without the mutation; the TypeScript target moves to ES2023 for it.</li><li><code>docs/reference/tools.md</code> is written by hand again. It used to be generated from the registered tools, which kept it in step with the code at the price of a page nobody could edit: <code>--check</code> compared it byte for byte, so every line had to be derivable and a paragraph about how an endpoint really behaves had nowhere to go. A test now asserts what the generator guaranteed — the page documents exactly the tools that exist, marks exactly the <code>essential</code> preset, and marks exactly the tools that ask a person first — and leaves the prose to a person.</li><li>Source maps are no longer published in the npm tarball. Node reads them only under <code>--enable-source-maps</code>, which nothing here sets, and the maps pointed at a <code>src/</code> this package does not ship — so a stack trace under that flag named a file nobody could open. <code>dist/**/*.js</code> is unchanged; the package is about a fifth smaller.</li><li>The docs workflow builds and deploys in two jobs, so <code>contents: write</code> is scoped to the deploy step rather than granted to the whole workflow.</li><li><code>oxlint</code> to 1.81.</li></ul><h2 id="_0-1-2-2026-09-06" tabindex="-1"><a href="https://github.com/ni-c/caldav-mcp/releases/tag/v0.1.2" target="_blank" rel="noreferrer">0.1.2</a> - 2026-09-06 <a class="header-anchor" href="#_0-1-2-2026-09-06" aria-label="Permalink to “0.1.2 - 2026-09-06”">​</a></h2><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><strong>A cross-host <code>/.well-known/caldav</code> redirect ended discovery instead of degrading past it.</strong> RFC 6764 §6 defines that route as a redirect and permits it to point at another host, which is how a hosted provider sends a client from the domain you typed to the one that serves DAV. Refusing to follow it is correct — following it would send the credentials somewhere you did not configure — but the refusal was thrown rather than returned, so discovery never reached its later steps or its home-set fallback. Because the principal is resolved once and memoised, that turned into the same error on <strong>every</strong> tool call for the life of the process. It is now a note naming the origin that was not followed, which is the address <code>CALDAV_URL</code> should have had.</li><li>The tool reference generator built its server from seven of <code>Config</code>&#39;s thirteen fields, under a comment claiming all of them. It now passes the whole record and asserts that every tool in the catalogue came back, so an incomplete literal fails the build instead of quietly producing a short page.</li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li><code>npm run docs:tools:check</code> runs in CI, which it never had, so the generated tool reference can no longer drift from the code.</li></ul><h3 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to “Documentation”">​</a></h3><ul><li><strong>The guide&#39;s three unfinished pages are written.</strong> <code>/guide/security</code> shipped as four empty headings — with the brief for writing one of them published as an HTML comment. <code>/guide/configuration</code> promised &quot;Getting a token&quot; and &quot;Required scopes&quot;, which CalDAV does not have at all. <code>/guide/clients</code> documented one of five clients, with a command that passes no credentials.</li><li>One authentication story everywhere: username and app-specific password, as the README always had it. Getting started, the clients page and the mcp-hub example had been describing a bearer token and scopes.</li><li><code>/reference/environment</code> lists all fourteen variables instead of eight, and no longer marks <code>CALDAV_TOKEN</code> as required.</li><li>The Glama badge, and both asset markers now name <code>svg-asset-set</code> rather than a generator script deleted some releases ago.</li></ul><h2 id="_0-1-0-2026-09-05" tabindex="-1"><a href="https://github.com/ni-c/caldav-mcp/releases/tag/v0.1.0" target="_blank" rel="noreferrer">0.1.0</a> - 2026-09-05 <a class="header-anchor" href="#_0-1-0-2026-09-05" aria-label="Permalink to “0.1.0 - 2026-09-05”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><p>First release. An MCP server for CalDAV: 22 tools over events, tasks and journal entries, on any server that speaks the standard.</p><ul><li><strong>22 tools</strong>, 10 read and 12 write. <code>CALDAV_READ_ONLY=true</code> leaves the write tools unregistered rather than failing them, so they do not appear in <code>tools/list</code> at all.</li><li><strong>Recurrence is expanded here, not by the server.</strong> Servers disagree about <code>expand</code>, and several get overrides wrong. The expansion walks the rule and then sweeps the overrides the rule never reaches, so an occurrence moved outside its own series still appears. Three independent bounds — the result cap, a per-series iteration cap and a wall-clock deadline — because <code>FREQ=SECONDLY</code> with no <code>UNTIL</code> is legal iCalendar. A window wider than 366 days is refused rather than quietly shortened: a truncated ten-year window looks exactly like &quot;nothing more in the calendar&quot;.</li><li><strong>Times carry their zone.</strong> Every timestamp is reported as an ISO 8601 instant, the original <code>TZID</code>, and an <code>all_day</code> flag. <code>TZID</code> names the platform does not know do not travel, and the document&#39;s own <code>VTIMEZONE</code> is never registered globally — a hostile entry that redefines <code>Europe/Berlin</code> affects only itself.</li><li><strong>Writes are read-modify-write over the parsed tree</strong>, never a document rebuilt from the fields this server models. Unknown <code>X-</code> properties, alarms, attachments, attendees and parameters survive because they are never touched. Guarded with <code>If-Match</code> from the same read; never <code>If-Match: *</code>; a weak ETag is refused; a 412 is <strong>not</strong> retried but answered with what the entry is now and the fact that nothing was written.</li><li><strong><code>CALDAV_CALENDARS</code> fences the server to named calendars</strong>, enforced where an id is decoded rather than at the edge of each tool, so no tool can forget it. A listing reports how many collections it withheld instead of quietly being shorter. An entry matching two calendars is refused at startup rather than resolved to whichever matched first.</li><li><strong>Ids are opaque and carry no origin.</strong> The host is rebuilt from <code>CALDAV_URL</code> on every decode, so a forged id cannot point this server at another server. Every join of a calendar URL and a resource name is checked on the <strong>resolved</strong> path: checking the name for a literal <code>/</code> is not the same check, because the URL parser normalises <code>%2E%2E</code> and treats a backslash as a separator. Names that would address something the id does not say — carrying <code>?</code>, <code>#</code>, a control character — are refused, while percent-encoded names still work.</li><li><strong>Calendar content is treated as content a stranger wrote</strong>, because on a server with scheduling anyone who knows your address can put an event in your calendar. Summaries, descriptions, locations, attendee names and calendar display names are fenced with a per-call nonce and marked line by line; invisible and directional characters are removed; markdown image syntax is defused so a rendering client cannot be induced to fetch a URL carrying data. Injection shapes are reported as a <strong>signal</strong>, never used to drop an entry.</li><li><strong>Nothing this server says quotes calendar content.</strong> Not the approval dialogs, not the error messages. Every value an error repeats — an id, a calendar name, an href, a timestamp — is escaped, collapsed to one line and cut first, because an error message reaches the model in the server&#39;s own voice, outside any fence.</li><li><strong>The dialogs describe what is actually there.</strong> Deleting reads the entry first and says whether it is one event or a recurring one and how many occurrences — including a resource made only of detached occurrences, which has no master to read a rule from. A series id with <code>scope: this_occurrence</code> is refused before anyone is asked rather than being shown one sentence and performing another. An approval is bound to the change as well as to the target, and a field left out is not the same as a field passed as <code>null</code>, so a yes to &quot;change the summary&quot; cannot execute &quot;change the summary and clear everything else&quot;.</li><li><strong>It never fetches an address somebody else chose.</strong> <code>ATTACH</code> URLs are reported and not retrieved; no tool takes a URL. Links returned by the server are pinned to the configured origin and refused if they carry credentials or a scheme this server does not speak — checked again at the point the credentials would leave the process.</li><li><strong><code>CALDAV_ALLOW_PLAINTEXT</code>.</strong> A plain <code>http://</code> URL to a host that is not loopback refuses to start instead of printing a warning that a stdio deployment never shows. The switch lifts the refusal and is read strictly, like <code>CALDAV_INSECURE_TLS</code>.</li><li>Bearer or Basic authentication, RFC 6764 discovery from a server root or a collection URL, <code>get_free_busy</code> with a client-side fallback where the server will not compute one, and full-text search per allowed calendar — never at the home set, which would reach every calendar underneath it.</li></ul>`,28)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};