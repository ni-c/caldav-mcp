import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CegG7-6m.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788654326000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>See the <a href="/reference/environment">environment variable reference</a> for the full table.</p><h2 id="signing-in" tabindex="-1">Signing in <a class="header-anchor" href="#signing-in" aria-label="Permalink to “Signing in”">​</a></h2><p>CalDAV authenticates the way the web did before OAuth: a username and a password, sent on every request. There is no authorisation flow to complete and there are <strong>no scopes</strong> — a point worth stating plainly, because MCP servers for hosted APIs mostly have both, and looking for a token page in your calendar provider&#39;s settings is a way to spend an afternoon finding nothing.</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_URL</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">https://dav.example.net</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_USERNAME</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">you</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_PASSWORD</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">your-app-password</span></span></code></pre></div><p><strong>Use an app-specific password where the provider offers one.</strong> Fastmail, iCloud, mailbox.org and most Nextcloud deployments do. It is not stronger cryptographically; it is revocable on its own, which means an MCP client&#39;s config file holding one is a thing you can turn off without changing the password you log in with. iCloud requires one — its account password will not authenticate against the CalDAV endpoint at all.</p><p><code>CALDAV_TOKEN</code> exists for the other shape: a deployment that puts a bearer-token proxy in front of a CalDAV server. It replaces the pair rather than joining it, and a configuration carrying both is refused at startup instead of picking one — which one was meant is not knowable, and the wrong guess authenticates as somebody else.</p><p>Whichever you use, what the account can do is what this server can do. Narrow it with <code>CALDAV_CALENDARS</code>, which names the collections this server may touch and makes everything else on the account invisible to it, and with <code>CALDAV_READ_ONLY</code> where nothing needs to write.</p><h2 id="finding-the-url" tabindex="-1">Finding the URL <a class="header-anchor" href="#finding-the-url" aria-label="Permalink to “Finding the URL”">​</a></h2><p>Point <code>CALDAV_URL</code> at the <strong>server root</strong> and let discovery walk from there: it asks for the principal, then for the calendar home set, the way a real client does. <code>https://dav.example.net</code> is usually the whole answer, and <code>get_server_info</code> reports what it found.</p><p>A <strong>collection URL</strong> works too and means something different — it short-circuits discovery to that one calendar, and nothing else on the account is reachable. That is a legitimate way to fence a deployment to a single calendar without an allowlist.</p><p>Where a provider publishes a well-known route, RFC 6764 lets it redirect to another host. This server does not follow a redirect to a different origin — that would send your credentials somewhere you did not configure — so if the route points elsewhere, set <code>CALDAV_URL</code> to where it points.</p><h2 id="tls" tabindex="-1">TLS <a class="header-anchor" href="#tls" aria-label="Permalink to “TLS”">​</a></h2><p><code>https://</code> everywhere except loopback, and no setting relaxes that quietly.</p><p><code>http://</code> to a host that is not <code>127.0.0.1</code> or <code>::1</code> <strong>refuses to start</strong>, and <code>CALDAV_ALLOW_PLAINTEXT=true</code> is what overrides it — deliberately spelled as an exact <code>true</code>, because Basic authentication puts the credentials on the wire in every single request and a typo must not be what turns that on. Plain HTTP to loopback needs nothing: the packets do not leave the machine.</p><p><code>CALDAV_INSECURE_TLS=true</code> accepts a self-signed certificate, which is the common case for a Radicale or Baikal instance on a home network. It is scoped to the configured host rather than switching validation off process-wide — no <code>NODE_TLS_REJECT_UNAUTHORIZED</code> here — so anything else this process talks to is still checked. It, too, takes the exact string <code>true</code>.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p>The guarded tools ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>CALDAV_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike the booleans above — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>CALDAV_ALLOW_TOOLS</code> and <code>CALDAV_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_events,get_event,create_event</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">CALDAV_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_event</span></span></code></pre></div><p>Why bother, when all of them work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. If this is the only MCP server in a session, the full set is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_thing</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset: <code>list_calendars</code>, <code>list_events</code>, <code>get_event</code>, <code>search_events</code>, <code>get_free_busy</code>, <code>create_event</code> and <code>update_event</code> — enough to see what is on, find a gap and put something in, with nothing irreversible in reach. It is marked per tool in the <a href="/reference/tools">tool reference</a>, generated from the same constant the filter reads, so the two cannot drift. It composes — naming a tool alongside it puts that one back, and <code>CALDAV_DENY_TOOLS</code> takes one away.</p><p><strong>Both together.</strong> <code>CALDAV_ALLOW_TOOLS</code> decides what is in; <code>CALDAV_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>CALDAV_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, which is what makes <code>get_*,create_*</code> a usable template for both kinds of deployment; and <code>CALDAV_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>CALDAV_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,29)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};