{
  "$comment": "Copy for docs/public/og.png, the GitHub social preview and the OpenGraph card. Rendered by scripts/gen-assets.mjs. Tagline and badges are split by hand because the renderer does not wrap text. Optional \"title\" overrides the heading — use it for a scoped package, where package.json's name reads as @ni-c/thing but the card should say thing.",
  "tagline": [
    "{{TAGLINE_LINE_1}}",
    "{{TAGLINE_LINE_2}}"
  ],
  "badges": ["{{TOOL_COUNT}} tools", "MIT", "Node ≥22"]
}
